pluginManagement {
    repositories {
        mavenLocal()
        maven {
            url = uri("https://maven.railroadide.dev/releases")
        }
        gradlePluginPortal()
    }
}

rootProject.name = 'RailroadDiscordPlugin'